# 아이디어 설명

## 한 줄 컨셉

매일 긴 자기소개서를 쓰게 하는 CLI가 아니라, 30초짜리 기억 조각이나 긴 초안도 나중에 쓸 수 있는 자소서 카드로 바꿔주는 로컬 AI 훈련기입니다.

기술 구현 방식, 설정 파일 구조, 로컬 LLM 호출 방식은 [docs/기술세부설명.md](docs/기술세부설명.md)를 참고하세요.

## 왜 필요한가

하루 1문답 훈련은 좋은 방식이지만, 사용자는 매일 새로운 소재를 떠올리는 데서 쉽게 막힙니다. 듀오링고처럼 매일 알림이 와도 막상 실행하면 귀찮은 이유는 오늘 당장 완성된 결과를 만들어야 한다는 부담이 크기 때문입니다.

이 기능의 목표는 성공 기준을 낮추는 것입니다.

```text
오늘의 성공 기준:
완성 답변 1개 또는 소재 씨앗 1개 저장
```

사용자가 긴 글을 쓰지 못해도 소재를 저장할 수 있고, 길게 쓴 초안이 있으면 원문을 최대한 살려 자소서 카드로 정리합니다.

## 최종 사용자 흐름

```text
1. CLI 시작
2. 자소서 준비도 지도 표시
3. 오늘의 3분 미션 추천
4. 오늘의 질문 생성
5. 질문 의도 3줄 표시
6. 사용자 자유 입력
7. 입력 타입 자동 분기
8. 부족한 핵심 1개만 질문
9. 최종 문단 생성 또는 긴 답변 보존 rewrite
10. 재평가
11. 상승폭 낮으면 retry 1회
12. 자소서 카드 저장
13. 준비도 변화 표시
14. 다음에 재활용 가능한 관점 추천
```

## 입력 부담 줄이기

사용자에게 STAR 칸을 하나씩 채우게 하지 않습니다. 먼저 기억나는 대로 한 번만 쓰게 합니다.

```text
문장 완성 안 해도 됩니다.
키워드만 적어도 됩니다.
오늘은 소재 하나만 건져가도 성공입니다.
```

CLI는 입력 길이와 내용을 보고 자동으로 처리합니다.

```text
30초 소재 입력
→ 소재 냉장고 저장

짧은 자유 덤프
→ STAR/도구/수치 자동 추출
→ 부족한 핵심 1개 질문
→ 자소서 문단 생성

일반 답변
→ 구조화 후 부족한 부분만 보강
→ 자소서 문단 생성

긴 답변
→ 원문 보존 모드
→ 자동 목표 길이 판단
→ 개선 요약 3줄
→ 원문 + 개선본 버전 저장
```

## 소재 고갈 방지

사용자가 `없음`, `모름`, `n`처럼 입력하면 바로 실패 처리하지 않고 기억 탐색기로 전환합니다.

```text
최근에 이런 적 있었나요?

[1] 반복 작업을 줄인 일
[2] 실수해서 다시 고친 일
[3] 누군가를 도와준 일
[4] 처음 배운 기술
[5] 팀에서 의견이 갈린 일
```

완성된 답변이 아니어도 한 줄 소재를 저장합니다.

```text
소재 냉장고:
인턴 때 SQL 리포트 자동화한 일
```

나중에는 이 소재를 문제해결, 주도성, 협업 같은 다른 관점으로 확장할 수 있습니다.

## 자소서 준비도 지도

단순 점수 대신 현재 준비 상태를 보여줍니다.

```text
자소서 준비도

경험 다양성      ██████░░░░ 60%
정량 성과        ███░░░░░░░ 30%
기술 키워드      ███████░░░ 70%
STAR 구조        █████░░░░░ 50%
직무 연관성      ██████░░░░ 60%
```

시작할 때는 부족한 영역을 기준으로 오늘의 미션을 추천하고, 종료할 때는 오늘 저장한 카드가 준비도를 어떻게 바꿨는지 보여줍니다.

## 긴 답변 처리

긴 글을 쓴 사용자의 노력을 보호합니다. 사용자가 이미 쓴 초안을 AI가 마음대로 갈아엎는 느낌을 줄이기 위해 원문과 개선본을 모두 저장합니다.

처리 방식:

```text
긴 답변 감지
→ 소재 분리 가능 여부 분석
→ 자동 목표 길이 판단
→ 원문 보존 rewrite
→ 개선 요약 3줄 표시
→ 원문 + 개선본 버전 저장
```

목표 글자 수는 사용자에게 묻지 않고 CLI가 자동 판단합니다.

## 저장 결과

저장 단위는 단순 답변이 아니라 자소서 카드입니다.

```text
핵심 경험
STAR 구조
사용 기술/도구
정량 성과
개선 전후 점수
원문/개선본 버전
재활용 가능한 역량 관점
```

이렇게 저장하면 사용자는 매일 완성된 자기소개서를 쓰지 않아도, 나중에 조합할 수 있는 경험 자산을 쌓을 수 있습니다.

## 로컬 검증 상태

2026-06-08 기준으로 `jintae_v2` 브랜치에서 아래 흐름을 로컬로 확인했습니다.

```text
Ollama exaone3.5:7.8b 모델 확인
→ EXAONE 단일 generate 호출 성공
→ CLI 전체 흐름 실행
→ 자유 입력 구조화
→ 부족한 핵심 질문 1개 생성
→ rewrite
→ retry UI
→ 자소서 카드 저장
→ 준비도 변화 출력
```

상세 테스트 결과는 [docs/기술세부설명.md](docs/기술세부설명.md)의 "로컬 테스트 결과" 섹션을 참고하세요.
