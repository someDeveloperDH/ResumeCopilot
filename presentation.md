# JobFit — 채용공고 기반 자기소개서 자동 최적화 CLI 도구

> NLP 기반 키워드 분석과 STAR 재구성을 통한 자소서 맞춤 수정 시스템

---

## 1. 배경 및 문제 정의

### 현실적인 문제
- 취업 준비생 대부분이 하나의 자소서를 여러 공고에 그대로 제출
- 채용공고마다 요구하는 역량과 키워드가 다름
- 공고를 분석하고 자소서를 수정하는 과정이 수동적이고 시간 소모적

### 핵심 질문
> "내 마스터 자소서가 이 공고에 얼마나 맞는가?
> 어느 부분을 어떻게 고쳐야 하는가?"

---

## 2. 프로젝트 목표

마스터 자소서 + 채용공고(URL 또는 파일)를 입력받아

1. 공고 텍스트를 **자동 수집 및 파싱**하고
2. 공고의 핵심 키워드를 **자동 추출**하고
3. 자소서 문단별로 공고와의 **부합도를 수치로 측정**하고
4. 부족한 부분을 **STAR 구조로 재작성 제안**하고
5. 사용자가 CLI에서 **승인 / 거절 / 수정 요청**으로 최종 확정

**출력: 공고 맞춤형 수정 자기소개서**

---

## 3. End-to-End 전체 흐름

```
사용자 입력
  ├── 마스터 자소서 (파일: .txt / .pdf)
  └── 채용공고     (URL  또는  파일: .txt)
          │
          ▼
┌─────────────────────────────────────────────────────────────────┐
│ STEP 0  입력 수집                                                │
│   URL → 스크래핑 → 텍스트 추출                                   │
│   파일 → 포맷 파싱 → 텍스트 추출                                 │
│   [평가] 수집 성공 여부 / 텍스트 길이 / 섹션 탐지 여부            │
└──────────────────────────┬──────────────────────────────────────┘
                           │
          ┌────────────────┴────────────────┐
          ▼                                 ▼
┌──────────────────┐             ┌──────────────────────┐
│ STEP 1           │             │ STEP 2               │
│ 채용공고 키워드   │             │ 마스터 자소서 문단 분리│
│ 추출             │             │ \n{2,} 기준          │
│ Rule + AI 비교   │             │                      │
│ [평가] 키워드 수  │             │ [평가] 문단 수        │
│ 방법 간 overlap  │             │ 평균 길이 / 이상 탐지 │
└────────┬─────────┘             └──────────┬───────────┘
         │                                  │
         └──────────────┬───────────────────┘
                        ▼
┌─────────────────────────────────────────────────────────────────┐
│ STEP 3  문단별 비교 분석 (병렬)                                   │
│                                                                  │
│   Jaccard ─┐                                                     │
│   TF-IDF  ─┤ ThreadPoolExecutor  +  KoSBERT 배치 추론 (GPU)     │
│   BM25    ─┘                                                     │
│                                                                  │
│   → ComparisonResult (문단별 수치 산출)                           │
│                                                                  │
│ [평가] 방법별 점수 / 문단별 점수 분포 / 커버리지 / 점수 분산      │
└──────────────────────────┬──────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────────┐
│ STEP 4  피드백 생성 & STAR 재구성                                 │
│   scorer  → 수치 기반 피드백 텍스트 생성                          │
│   rewriter → LLM 호출 → STAR 구조 재작성                         │
│                                                                  │
│ [평가] 재구성 전후 유사도 변화 / 키워드 커버리지 변화 / STAR 충족  │
└──────────────────────────┬──────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────────┐
│ STEP 5  CLI 대화형 루프                                           │
│   문단별 순서 진행: y / n / r / s                                 │
│                                                                  │
│ [평가] 세션 로그 / 사용자 선택 분포 / 재생성 횟수                  │
└──────────────────────────┬──────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────────┐
│ STEP 6  최종 평가 & 저장                                          │
│   수정 전후 전체 유사도 비교 / 리포트 출력 / 파일 저장             │
│                                                                  │
│ [평가] 전체 향상도 / 수정 문단 수 / 최종 커버리지                  │
└─────────────────────────────────────────────────────────────────┘
```

---

## 4. 단계별 구현 및 평가

---

### STEP 0 — 입력 수집

**목적:** 채용공고와 자소서를 텍스트로 변환

```
입력 유형
  ├── 채용공고
  │     ├── URL   → requests + BeautifulSoup → 텍스트 추출
  │     └── .txt  → 직접 읽기
  │
  └── 마스터 자소서
        ├── .txt  → 직접 읽기
        └── .pdf  → pdfminer / PyPDF2 → 텍스트 추출
```

```
URL 스크래핑 흐름:
  URL 입력
    → robots.txt 확인
    → HTML 다운로드 (requests)
    → 불필요 태그 제거 (script, style, nav)
    → 본문 영역 탐지 (BeautifulSoup)
    → 채용공고 섹션 파싱 ([주요업무] [자격요건] [우대사항])
    → 텍스트 정제
```

#### 평가 지표 (STEP 0)

| 지표 | 설명 | 판단 기준 |
|---|---|---|
| `fetch_success` | 스크래핑 성공 여부 | True / False |
| `text_length` | 추출된 텍스트 길이 | 200자 이상이면 정상 |
| `section_detected` | 주요업무/자격요건/우대사항 탐지 여부 | 1개 이상 탐지 |
| `cover_paragraphs` | 자소서 문단 수 | 1개 이상 |

```
[STEP 0 평가 출력 예시]
━━━━━━━━━━━━━━━━━━━━━━━━━━━━
✅ 채용공고 수집 완료
   - 출처: URL (스크래핑)
   - 텍스트 길이: 842자
   - 탐지된 섹션: 주요업무 ✓  자격요건 ✓  우대사항 ✓

✅ 마스터 자소서 로드 완료
   - 포맷: .txt
   - 텍스트 길이: 1,204자
━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

---

### STEP 1 — 채용공고 키워드 추출

**목적:** 공고에서 중요한 단어만 뽑아 이후 비교의 기준으로 사용

```
채용공고 텍스트
    │
    ├─ 전처리: 특수문자 제거, 형태소 분석 (KoNLPy)
    │
    ├─ [방법 A] Rule-based
    │    품사 필터링 — 명사(NNG), 외래어(SL), 동사(VV)
    │    "데이터 파이프라인 설계 및 구축"
    │    → [데이터, 파이프라인, 설계, 구축]
    │
    └─ [방법 B] AI-based
         KeyBERT — ko-BERT 임베딩 기반 중요도 순 추출
         → [파이프라인(0.91), ETL(0.88), SQL(0.85)]

→ 두 결과 비교 후 최적 선택 또는 합산
→ 섹션별 가중치 부여: 주요업무 > 자격요건 > 우대사항
```

#### 평가 지표 (STEP 1)

| 지표 | 설명 | 판단 기준 |
|---|---|---|
| `n_keywords` | 섹션별 추출 키워드 수 | 섹션당 3개 이상 |
| `rule_ai_overlap` | 두 방법 결과 겹치는 비율 | 40% 이상이면 신뢰 |
| `unique_keywords` | 중복 제거 후 전체 키워드 수 | 10개 이상 |
| `top_keywords` | 상위 키워드 미리보기 | 사용자 육안 확인용 |

```
[STEP 1 평가 출력 예시]
━━━━━━━━━━━━━━━━━━━━━━━━━━━━
✅ 키워드 추출 완료
   주요업무 (5): 파이프라인, ETL, SQL, 데이터품질, 모니터링
   자격요건 (4): Python, SQL, 데이터처리, 문제해결
   우대사항 (4): Spark, AWS, 클라우드, 협업

   Rule-based: 16개  AI-based: 14개  Overlap: 78%
   → 선택: 합산 방식 (전체 18개)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

---

### STEP 2 — 자소서 문단 분리

**목적:** 마스터 자소서를 처리 단위인 문단으로 자동 분리

```python
def split_paragraphs(text: str) -> list[str]:
    paragraphs = re.split(r'\n{2,}', text)
    return [p.strip() for p in paragraphs if p.strip()]
# 이 함수만 교체하면 자동 탐지 방식으로 전환 가능
```

#### 평가 지표 (STEP 2)

| 지표 | 설명 | 판단 기준 |
|---|---|---|
| `n_paragraphs` | 분리된 문단 수 | 1개 이상 |
| `avg_length` | 평균 문단 길이 (자) | 50자 이상 |
| `short_paragraphs` | 30자 미만 문단 | 경고 출력 |
| `long_paragraphs` | 500자 초과 문단 | 경고 출력 |

```
[STEP 2 평가 출력 예시]
━━━━━━━━━━━━━━━━━━━━━━━━━━━━
✅ 문단 분리 완료 (총 4개)
   문단 0: 198자  [지원동기 추정]
   문단 1: 312자  [경험 추정]
   문단 2: 187자  [경험 추정]
   문단 3: 224자  [포부 추정]
   평균 길이: 230자  /  이상 문단: 없음
━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

---

### STEP 3 — 채용공고 ↔ 자소서 비교 (핵심)

**목적:** 각 문단이 공고와 얼마나 맞는지 수치로 측정
→ 이 수치가 STEP 4 (재구성)의 입력이 됨

#### 4가지 비교 방법 및 역할

| 방법 | 역할 | 산출 값 |
|---|---|---|
| Jaccard | 키워드 집합 겹침 | covered / missing / coverage_ratio |
| TF-IDF + Cosine | 전체 텍스트 유사도 | overall_score 기반 |
| BM25 | 섹션별 관련도 | section_scores |
| KoSBERT | 의미 기반 보정 | overall_score 보정 |

#### 병렬 실행 구조

```
시작 시   → KoSBERT 전체 문단 배치 추론 (GPU, 캐시 저장)

문단 루프 → Jaccard  ─┐
            TF-IDF   ─┤ ThreadPoolExecutor (CPU 병렬)
            BM25     ─┘
            KoSBERT  → 캐시 조회 (GPU 대기 없음)
                │
                ▼
          ComparisonResult 생성
```

#### ComparisonResult 구조

```python
@dataclass
class ComparisonResult:
    paragraph_idx:     int        # 문단 번호
    paragraph_text:    str        # 원문
    overall_score:     float      # 전체 유사도 (0~1)
    section_scores:    dict       # {주요업무: 0.8, 자격요건: 0.7, 우대사항: 0.2}
    dominant_section:  str        # 가장 높은 섹션
    covered_keywords:  list[str]  # 포함된 키워드
    missing_keywords:  list[str]  # 누락 키워드
    priority_keywords: list[str]  # 누락 중 우선순위 높은 것
    coverage_ratio:    float      # covered / total
    fix_urgency:       str        # "high" | "medium" | "low"
    needs_rewrite:     bool       # overall_score < threshold
```

#### 평가 지표 (STEP 3)

| 지표 | 설명 | 판단 기준 |
|---|---|---|
| `per_method_scores` | 방법별 점수 각각 | 방법 간 차이 0.3 이하이면 신뢰 |
| `score_variance` | 4개 방법 점수 분산 | 낮을수록 안정적 |
| `coverage_ratio` | 키워드 커버리지 | 문단별 비교 |
| `score_distribution` | 전체 문단 점수 분포 | 고르게 분포가 이상적 |

```
[STEP 3 평가 출력 예시]
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
문단별 비교 결과:

       Jaccard  TF-IDF   BM25   KoSBERT  최종점수  긴급도
문단0   0.29    0.33     0.31    0.34      0.31    HIGH
문단1   0.71    0.72     0.75    0.76      0.74    LOW
문단2   0.00    0.09     0.06    0.11      0.08    HIGH
문단3   0.41    0.44     0.48    0.47      0.45    MED

전체 평균 유사도: 0.40
수정 필요 문단: 3개 (문단 0, 2, 3)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

---

### STEP 4 — 피드백 생성 & STAR 재구성

**목적:** ComparisonResult를 바탕으로 실제 수정 문장 생성

```
입력: ComparisonResult
    │
    ├─ [scorer.py]
    │    수치 → 사람이 읽는 피드백 텍스트 변환
    │    "공고 부합도 ★☆☆☆☆ (0.08)
    │     주요업무와 관련 낮음 — 핵심 키워드 없음
    │     삽입 필요: 파이프라인, SQL, ETL (우선순위 순)"
    │
    └─ [rewriter.py]
         priority_keywords + dominant_section + 원문 → LLM

         S (Situation): 배경 상황
         T (Task):      맡은 역할
         A (Action):    구체적 행동 + 누락 키워드 자연 삽입
         R (Result):    정량적 성과
```

#### 평가 지표 (STEP 4)

| 지표 | 설명 | 판단 기준 |
|---|---|---|
| `score_delta` | 재구성 후 유사도 변화 | +0.1 이상이면 개선 |
| `coverage_delta` | 재구성 후 키워드 커버리지 변화 | 증가해야 함 |
| `star_fulfilled` | S/T/A/R 구조 포함 여부 | 4개 요소 모두 포함 |
| `keyword_injected` | priority_keywords 실제 삽입 여부 | 1개 이상 삽입 |

```
[STEP 4 평가 출력 예시]
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
문단 2 재구성 평가:
  유사도:   0.08 → 0.61  (+0.53) ✅
  커버리지: 0%   → 55%   (+55%)  ✅
  STAR 구조: S✓  T✓  A✓  R✓      ✅
  삽입 키워드: 파이프라인, SQL, ETL, Python ✅
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

---

### STEP 5 — CLI 대화형 루프

**목적:** 사용자가 문단별로 수정 결과를 검토하고 확정

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[문단 3/4]  공고 부합도: ★☆☆☆☆ (0.08)  [HIGH]
누락 키워드: 파이프라인, SQL, ETL, Python
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[원문]
학부 시절 교내 동아리에서 웹 서비스 개발 프로젝트를
진행했습니다. React와 Node.js를 사용하여...

[제안 — STAR 재구성]
데이터 처리 효율화가 필요한 환경에서(S) ETL 파이프라인
구축을 담당하여(T) Python과 SQL을 활용해 데이터 수집
자동화를 설계하고(A) 처리 시간을 60% 단축했습니다.(R)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
> [y] 적용  [n] 재생성  [r] 요구사항 입력  [s] 건너뜀
```

| 입력 | 동작 |
|---|---|
| `y` | 수정본 저장, 다음 문단 |
| `n` | LLM 재호출, 새 제안 생성 |
| `r` | 요구사항 입력 받아 반영 후 재생성 |
| `s` | 원문 유지, 다음 문단 |

#### 평가 지표 (STEP 5)

| 지표 | 설명 |
|---|---|
| `session_log` | 문단별 사용자 선택 기록 (y/n/r/s) |
| `regen_count` | 재생성 요청 횟수 |
| `user_request_log` | r 선택 시 입력한 요구사항 기록 |

---

### STEP 6 — 최종 평가 & 저장

**목적:** 전체 수정 결과를 수치로 요약하고 사용자가 저장 결정

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📊 최종 평가 리포트
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
전체 유사도:  0.40 → 0.68  (+70% 향상)
키워드 커버리지: 38% → 72%  (+34%p)
수정된 문단: 3/4개

문단별 결과:
  문단 0 (지원동기):  수정됨  0.31 → 0.52  ✓
  문단 1 (ETL 경험):  원문 유지 (이미 높음, n 선택)
  문단 2 (웹개발):    수정됨  0.08 → 0.61  ✓
  문단 3 (포부):      수정됨  0.45 → 0.58  ✓
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
> [w] 저장   [p] 전문 미리보기   [d] 취소
```

#### 평가 지표 (STEP 6)

| 지표 | 설명 | 판단 기준 |
|---|---|---|
| `overall_score_before` | 수정 전 평균 유사도 | 기준선 |
| `overall_score_after` | 수정 후 평균 유사도 | 높을수록 좋음 |
| `improvement_pct` | 향상률 (%) | 20% 이상이면 의미 있음 |
| `coverage_before` | 수정 전 키워드 커버리지 | 기준선 |
| `coverage_after` | 수정 후 키워드 커버리지 | 높을수록 좋음 |
| `paragraphs_modified` | 수정된 문단 수 | 전체 대비 비율 |

---

### STEP 7 — AI 작성 기여도 분석

**목적:** 최종 자소서에서 AI가 실제로 얼마나 개입했는지를 다각도로 측정

#### 측정 관점 3가지

```
관점 1. 텍스트 변화량     — 원문 대비 최종본이 얼마나 달라졌는가
관점 2. 세션 행동 로그    — 사용자가 얼마나 직접 개입했는가
관점 3. 언어 스타일 분석  — 문체·어휘가 AI 특성으로 바뀌었는가
```

---

#### 관점 1 — 텍스트 변화량 (Edit Distance 기반)

```
원문 문단  ←→  최종 문단
         편집 거리 계산 (Levenshtein)

변화율(%) = 편집 거리 / max(원문 길이, 최종 길이) × 100

변화율 0%    → 원문 그대로 (AI 개입 없음)
변화율 1~40% → 부분 수정 (사용자 + AI 혼합)
변화율 41~79%→ 대폭 수정 (AI 주도)
변화율 80%+  → 사실상 전면 재작성 (AI 작성)
```

---

#### 관점 2 — 세션 행동 로그 기반

```
STEP 5 루프에서 축적된 선택 기록 활용

  y (AI 제안 그대로 수락)    → AI 기여도 높음
  n (재생성 요청)             → AI 주도, 사용자 검토
  r (요구사항 입력 후 수락)   → 사용자 + AI 공동 작성
  s (건너뜀, 원문 유지)       → AI 기여 없음

문단별 사용자 개입 점수:
  y → 개입도 0.1  (거의 AI)
  n → 개입도 0.3  (AI 주도, 사용자 검토)
  r → 개입도 0.6  (공동 작성)
  s → 개입도 1.0  (사용자 원문)
```

---

#### 관점 3 — 언어 스타일 분석

```
분석 항목:
  ① Perplexity (언어 모델 기반)
      낮을수록 AI가 생성할 법한 매끄러운 문장
      높을수록 사람이 쓴 자연스러운 불규칙성 존재

  ② 어휘 다양성 (TTR, Type-Token Ratio)
      원문 TTR vs 최종본 TTR 비교
      AI 생성 텍스트는 TTR이 낮은 경향

  ③ 평균 문장 길이
      AI 생성 텍스트는 문장이 균일하게 길어지는 경향
```

---

#### 최종 AI 기여도 산출

```
문단별 AI 기여도 = 가중 평균(변화율, 행동점수, 스타일점수)

전체 AI 기여도 = 문단별 기여도 × 문단 길이 비율의 합

분류:
  0  ~ 20%: 자필 중심  (AI 보조)
  21 ~ 50%: 혼합 작성  (사용자 + AI)
  51 ~ 80%: AI 주도    (사용자 검토)
  81 ~100%: AI 작성    (사용자 확인만)
```

---

#### 평가 지표 (STEP 7)

| 지표 | 설명 | 산출 방법 |
|---|---|---|
| `para_edit_ratio` | 문단별 텍스트 변화율 (%) | Levenshtein / max길이 |
| `para_user_intervention` | 문단별 사용자 개입도 | 세션 선택 기반 |
| `perplexity_delta` | 원문 vs 최종본 Perplexity 변화 | KoGPT 또는 KoLM |
| `ttr_delta` | 어휘 다양성 변화 (TTR) | 형태소 분석 후 계산 |
| `avg_sent_len_delta` | 평균 문장 길이 변화 | 문장 분리 후 계산 |
| `ai_contribution_score` | 최종 AI 기여도 (0~100%) | 3가지 관점 가중 평균 |

---

#### 출력 예시

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🤖 AI 작성 기여도 분석 리포트
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

문단별 분석:

        변화율   사용자개입  스타일변화  AI기여도   판정
문단 0   38%      y(0.1)     중간        42%      혼합 작성
문단 1    0%      s(1.0)     없음         0%      자필 (원문 유지)
문단 2   91%      y(0.1)     높음        87%      AI 작성
문단 3   44%      r(0.6)     낮음        31%      혼합 작성

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📊 전체 AI 기여도: 42%  →  [혼합 작성]

언어 스타일 변화:
  Perplexity:     128.4 → 94.1   (AI 특성으로 변화)
  어휘 다양성:    0.71  → 0.63   (다소 균일해짐)
  평균 문장 길이: 38자  → 51자   (AI 특성으로 길어짐)

세션 요약:
  y(즉시 수락) 1회  /  n(재생성) 0회
  r(요구반영)  1회  /  s(건너뜀) 1회
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

---

## 5. 폴더 구조

```
test1/
├── main.py                      # CLI 진입점
├── presentation.md              # 발표 자료
├── data/
│   ├── sample_job.txt           # 채용공고 예시
│   └── sample_cover.txt         # 마스터 자소서 예시
├── src/
│   ├── input/                   # (신규) STEP 0
│   │   ├── fetcher.py           # URL 스크래핑
│   │   └── parser.py            # 파일 파싱 (.txt / .pdf)
│   ├── preprocessing/           # STEP 1, 2
│   │   ├── cleaner.py
│   │   └── tokenizer.py
│   ├── keyword/                 # STEP 1
│   │   ├── rule_based.py
│   │   ├── ai_based.py
│   │   └── comparator.py
│   ├── similarity/              # STEP 3
│   │   ├── schema.py            # ComparisonResult 정의
│   │   ├── jaccard.py
│   │   ├── tfidf.py
│   │   ├── bm25.py
│   │   ├── sbert.py
│   │   └── aggregator.py
│   ├── feedback/                # STEP 4
│   │   ├── scorer.py
│   │   └── rewriter.py
│   ├── cli/                    # STEP 5, 6
│   │   ├── display.py
│   │   └── session.py
│   └── analysis/               # (신규) STEP 7
│       ├── edit_distance.py    # 텍스트 변화율 (Levenshtein)
│       ├── style_analyzer.py   # Perplexity, TTR, 문장 길이
│       └── ai_reporter.py      # 3가지 관점 통합 → AI 기여도 리포트
└── logs/
    └── changelog.md
```

---

## 6. 기술 스택

| 영역 | 기술 |
|---|---|
| URL 스크래핑 | `requests` + `BeautifulSoup4` |
| PDF 파싱 | `pdfminer.six` / `PyPDF2` |
| 형태소 분석 | `KoNLPy` (Okt / Mecab) |
| 키워드 추출 | `KeyBERT`, 품사 필터 |
| 유사도 측정 | `sklearn` (TF-IDF), `rank_bm25`, `sentence-transformers` |
| 문장 임베딩 | `KoSBERT` (`snunlp/KR-SBERT-V40K-klueNLI-augSTS`) |
| 병렬 처리 | `ThreadPoolExecutor`, CUDA batch inference |
| LLM 재구성 | Claude API (`claude-haiku-4-5`) |
| CLI 인터페이스 | `Click` + `Rich` |
| 편집 거리 | `python-Levenshtein` |
| Perplexity 측정 | `KoGPT2` (skt/kogpt2-base-v2) |

---

## 7. 차별점

| 기존 방식 | 이 프로젝트 |
|---|---|
| 사람이 수동으로 공고-자소서 비교 | 수치 기반 자동 분석 |
| 감으로 수정 | 누락 키워드 명시 + 우선순위 제시 |
| 전체 재작성 | 문단 단위 선택적 수정 |
| 단일 방법론 | 4가지 NLP 방법 병렬 비교 |
| 평가 기준 없음 | 단계별 정량 평가 지표 내장 |
| URL 수동 복붙 | URL 직접 입력 → 자동 스크래핑 |
| AI 개입 불투명 | AI 기여도 정량 분석 리포트 제공 |

---

## 8. 현재 진행 상태

| 단계 | 상태 |
|---|---|
| STEP 0 — 입력 수집 (URL/파일) | 🔵 설계 완료 / 미구현 |
| STEP 1 — 키워드 추출 | 🔵 설계 완료 / 미구현 |
| STEP 2 — 문단 분리 | 🔵 설계 완료 / 미구현 |
| STEP 3 — 비교 모듈 | 🔵 설계 완료 / 미구현 |
| STEP 4 — 피드백 & 재구성 | 🔵 설계 완료 / 미구현 |
| STEP 5 — CLI 루프 | 🔵 설계 완료 / 미구현 |
| STEP 6 — 최종 평가 & 저장 | 🔵 설계 완료 / 미구현 |
| STEP 7 — AI 기여도 분석 | 🔵 설계 완료 / 미구현 |
| 테스트 데이터 | ✅ 완료 |
